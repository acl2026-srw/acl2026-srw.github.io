---
export interface Props {
  /** # of columns at medium breakpoint (1–6) */
  cols?: 1|2|3|4|5|6;
}
const { cols = 3 } = Astro.props;
const gridCols = `organizer-grid organizer-grid--cols-${cols}`;
---
<div class={gridCols}>
  <slot />
</div>